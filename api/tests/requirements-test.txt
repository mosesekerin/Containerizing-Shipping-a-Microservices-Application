# Test dependencies — never installed in production images.
# These are ONLY used in CI during the test stage.
#
# pytest       : test runner
# pytest-cov   : measures how much of your code the tests cover
# httpx        : async HTTP client required by FastAPI's TestClient
# fakeredis    : an in-memory Redis fake — no real Redis needed in tests
pytest==8.3.5
pytest-cov==6.1.0
httpx==0.28.1
fakeredis==2.28.1